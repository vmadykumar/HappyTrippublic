pipeline {
        agent { label 'docker'}
        stages {
                stage('checkout'){
                        
                        steps {
                               echo 'starting checkout'
                        }
                }
        }
}
